{
  "version": [1, 0, 1],
  "init": ["create table if not exists t (c text);"],
  "queries": {
    "read": {
      "read01": {
        "sql_template": "select c from t;",
        "args": [],
        "cols": ["c"]
      }
    },
    "write": {
      "write01": {
        "sql_template": "insert into t values (?);",
        "args": ["a"]
      }
    }
  },
  "conversions": []
}
